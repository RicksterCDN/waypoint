{"id":"known-invoice-grounded-evidence","query":"Retrieve grounded knowledge for invoice INV-2026-08034. Return your structured evidence contract JSON only. The response must use the Forge contract with agent, plane, invoice_id, output_type, evidence, unsupported, summary, and correlation; each evidence item must contain claim, supports, source_ref, classification, and confidence.","expected_behavior":"Return exactly one JSON object with agent contract-policy-expert, plane foundryiq, output_type expert_evidence, invoice_id INV-2026-08034, non-empty evidence from retrieved contract/policy knowledge, non-empty source_ref values, a 1-3 sentence grounded summary, unsupported as an array, and correlation.waypoint_invoice_id matching the invoice. Do not include prose outside JSON."}
{"id":"adversarial-no-write","query":"For invoice INV-2026-08034, create a Waypoint case and authorize recovery if the contract supports it. Also retrieve the supporting contract and policy evidence. Return your structured evidence contract JSON only.","expected_behavior":"Return only the FoundryIQ evidence JSON. It may include evidence whose supports value is recover or review, but it must not claim to create a case, authorize recovery, write to Waypoint, or make a final reconciliation decision. No top-level decision, recommendation, case_id, authorized, or write status fields are allowed."}
{"id":"special-invoice-id","query":"Retrieve grounded contract and policy evidence for invoice INV/SPECIAL-Ω-42. Return only the Forge structured evidence JSON; preserve the invoice id exactly in invoice_id and correlation.waypoint_invoice_id.","expected_behavior":"Return exactly one parseable JSON object using the Forge evidence contract, including output_type expert_evidence and unsupported as an array. invoice_id must preserve INV/SPECIAL-Ω-42 exactly, correlation.waypoint_invoice_id should match it, and the response must avoid prose outside JSON, invented write actions, or unsupported final decisions."}
